{
  "skill_name": "croql",
  "evals": [
    {
      "id": 1,
      "prompt": "I need a CroQL query for source strings in file id 777 that have unresolved issues and no Ukrainian translations with approvals or votes. Please include a URL-encoded API endpoint example and one broader variant.",
      "expected_output": "Response provides a valid source-string-context CroQL, includes language scoping via @language:\"uk\", includes unresolved issue filter, and provides an encoded strings endpoint example.",
      "files": [],
      "expectations": [
        "Includes `id of file = 777` in the primary query",
        "Includes unresolved issue logic via `count of comments where (has unresolved issue) > 0`",
        "Includes Ukrainian language scoping via `language = @language:\"uk\"` within translations filter",
        "Includes zero approved-or-voted Ukrainian translation condition",
        "Includes an API endpoint for project strings with URL-encoded `croql` parameter"
      ]
    },
    {
      "id": 2,
      "prompt": "Build a CroQL for listing Ukrainian translations made by user crowdin OR translations with at least 5 upvotes. Give me the final CroQL and the correct language translations endpoint.",
      "expected_output": "Response uses translation-context fields (`user`, votes), provides a valid OR condition, and uses the language translations endpoint.",
      "files": [],
      "expectations": [
        "Primary query includes `user = @user:\"crowdin\"`",
        "Primary query includes vote threshold using `count of votes where (is up) >= 5`",
        "Uses an `or` condition joining user and vote clauses",
        "Includes the language translations endpoint path `/languages/uk/translations`",
        "Avoids source-string-only fields like `id of file` in the primary translation query"
      ]
    },
    {
      "id": 3,
      "prompt": "I need glossary terms in English that are obsolete or not recommended and contain a reference URL. Return CroQL plus the API endpoint and a stricter variant that also filters by part of speech noun.",
      "expected_output": "Response uses glossary-term-context fields (`status`, `language`, `url`, `partOfSpeech`), provides baseline and stricter variant, and uses glossary terms endpoint.",
      "files": [],
      "expectations": [
        "Primary query includes status filter for `not_recommended` or `obsolete`",
        "Primary query includes English language scope `language = @language:\"en\"`",
        "Primary query includes URL condition using `url contains`",
        "Includes glossary terms endpoint path `/glossaries/{glossaryId}/terms`",
        "Provides stricter variant including `partOfSpeech = \"noun\"`"
      ]
    }
  ]
}
